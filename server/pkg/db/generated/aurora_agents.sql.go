// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: aurora_agents.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createAuroraManagedRuntime = `-- name: CreateAuroraManagedRuntime :one
INSERT INTO agent_runtime (
    workspace_id, daemon_id, name, runtime_mode, provider, status,
    device_info, metadata, owner_id, visibility
) VALUES (
    $1, NULL, $2, 'cloud', $3, 'offline', '', '{}'::jsonb, $4, 'private'
)
RETURNING id, workspace_id, daemon_id, name, runtime_mode, provider, status, device_info, metadata, last_seen_at, created_at, updated_at, owner_id, legacy_daemon_id, visibility, profile_id, custom_name
`

type CreateAuroraManagedRuntimeParams struct {
	WorkspaceID pgtype.UUID `json:"workspace_id"`
	Name        string      `json:"name"`
	Provider    string      `json:"provider"`
	OwnerID     pgtype.UUID `json:"owner_id"`
}

func (q *Queries) CreateAuroraManagedRuntime(ctx context.Context, arg CreateAuroraManagedRuntimeParams) (AgentRuntime, error) {
	row := q.db.QueryRow(ctx, createAuroraManagedRuntime,
		arg.WorkspaceID,
		arg.Name,
		arg.Provider,
		arg.OwnerID,
	)
	var i AgentRuntime
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.DaemonID,
		&i.Name,
		&i.RuntimeMode,
		&i.Provider,
		&i.Status,
		&i.DeviceInfo,
		&i.Metadata,
		&i.LastSeenAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.OwnerID,
		&i.LegacyDaemonID,
		&i.Visibility,
		&i.ProfileID,
		&i.CustomName,
	)
	return i, err
}

const getAuroraManagedRuntime = `-- name: GetAuroraManagedRuntime :one

SELECT id, workspace_id, daemon_id, name, runtime_mode, provider, status, device_info, metadata, last_seen_at, created_at, updated_at, owner_id, legacy_daemon_id, visibility, profile_id, custom_name FROM agent_runtime
WHERE workspace_id = $1
  AND daemon_id IS NULL
  AND runtime_mode = 'cloud'
  AND provider = $2
ORDER BY created_at ASC, id ASC
LIMIT 1
`

type GetAuroraManagedRuntimeParams struct {
	WorkspaceID pgtype.UUID `json:"workspace_id"`
	Provider    string      `json:"provider"`
}

// Aurora system-agent seeding (Plan 3 Task 1). These queries back
// aurora.EnsureSystemAgents, which lazily materialises a workspace's 16 skill
// system agents — one managed runtime row, and per catalog skill one
// kind='system' agent, one skill row, and the agent_skill junction — on first
// generation creation.
// The workspace's server-hosted (managed) runtime, the idempotency anchor for
// seeding. daemon_id is NULL because no daemon has claimed it yet; a sandbox
// daemon claims it later (Plan 3 Task 3). There is no unique constraint that
// covers a NULL daemon_id — migration 121's partial index keys
// (workspace_id, daemon_id, provider) and NULLs are distinct — so the seed
// looks it up rather than relying on an ON CONFLICT arbiter.
func (q *Queries) GetAuroraManagedRuntime(ctx context.Context, arg GetAuroraManagedRuntimeParams) (AgentRuntime, error) {
	row := q.db.QueryRow(ctx, getAuroraManagedRuntime, arg.WorkspaceID, arg.Provider)
	var i AgentRuntime
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.DaemonID,
		&i.Name,
		&i.RuntimeMode,
		&i.Provider,
		&i.Status,
		&i.DeviceInfo,
		&i.Metadata,
		&i.LastSeenAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.OwnerID,
		&i.LegacyDaemonID,
		&i.Visibility,
		&i.ProfileID,
		&i.CustomName,
	)
	return i, err
}

const upsertAuroraSkill = `-- name: UpsertAuroraSkill :one
INSERT INTO skill (workspace_id, name, description, content, config, created_by)
VALUES ($1, $2, '', $3, '{}'::jsonb, $4)
ON CONFLICT (workspace_id, name)
DO UPDATE SET content = EXCLUDED.content, updated_at = now()
RETURNING id, workspace_id, name, description, content, config, created_by, created_at, updated_at, plugin_installation_id
`

type UpsertAuroraSkillParams struct {
	WorkspaceID pgtype.UUID `json:"workspace_id"`
	Name        string      `json:"name"`
	Content     string      `json:"content"`
	CreatedBy   pgtype.UUID `json:"created_by"`
}

// Inserts or refreshes the skill row a system agent's skill resolves to. The
// name equals the agent's display name, and skill's UNIQUE(workspace_id, name)
// is the idempotency key. content is the minimal SKILL.md workflow text,
// refreshed on every seed so Plan 3.5's enrichment reaches already-seeded
// workspaces.
func (q *Queries) UpsertAuroraSkill(ctx context.Context, arg UpsertAuroraSkillParams) (Skill, error) {
	row := q.db.QueryRow(ctx, upsertAuroraSkill,
		arg.WorkspaceID,
		arg.Name,
		arg.Content,
		arg.CreatedBy,
	)
	var i Skill
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.Name,
		&i.Description,
		&i.Content,
		&i.Config,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.PluginInstallationID,
	)
	return i, err
}

const upsertAuroraSystemAgent = `-- name: UpsertAuroraSystemAgent :one
INSERT INTO agent (
    workspace_id, owner_id, runtime_id, kind, system_key, name, instructions,
    runtime_mode, visibility, permission_mode, runtime_config
) VALUES (
    $1, $2, $3, 'system', $4, $5, $6, 'cloud', 'workspace', 'private', '{}'::jsonb
)
ON CONFLICT (workspace_id, owner_id, runtime_id, system_key) WHERE system_key IS NOT NULL
DO UPDATE SET name = EXCLUDED.name, instructions = EXCLUDED.instructions
RETURNING id, workspace_id, name, avatar_url, runtime_mode, runtime_config, visibility, status, max_concurrent_tasks, owner_id, created_at, updated_at, description, runtime_id, instructions, archived_at, archived_by, custom_env, custom_args, mcp_config, model, thinking_level, composio_toolkit_allowlist, permission_mode, kind, system_key, disabled_runtime_skills, service_tier, conversation_starters
`

type UpsertAuroraSystemAgentParams struct {
	WorkspaceID  pgtype.UUID `json:"workspace_id"`
	OwnerID      pgtype.UUID `json:"owner_id"`
	RuntimeID    pgtype.UUID `json:"runtime_id"`
	SystemKey    pgtype.Text `json:"system_key"`
	Name         string      `json:"name"`
	Instructions string      `json:"instructions"`
}

// Inserts or refreshes one Aurora system agent. kind='system' marks it an
// invisible execution carrier (hidden from agent lists and hard-deleted with
// its runtime), exactly like the Agent Builder's carriers. Idempotency rides
// migration 172's partial unique index on
// (workspace_id, owner_id, runtime_id, system_key) WHERE system_key IS NOT
// NULL; the arbiter must name all four columns and repeat the predicate, or
// Postgres will not match the partial index. DO UPDATE refreshes
// name/instructions so a later seed enriches the prompt without creating a
// second row.
func (q *Queries) UpsertAuroraSystemAgent(ctx context.Context, arg UpsertAuroraSystemAgentParams) (Agent, error) {
	row := q.db.QueryRow(ctx, upsertAuroraSystemAgent,
		arg.WorkspaceID,
		arg.OwnerID,
		arg.RuntimeID,
		arg.SystemKey,
		arg.Name,
		arg.Instructions,
	)
	var i Agent
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.Name,
		&i.AvatarUrl,
		&i.RuntimeMode,
		&i.RuntimeConfig,
		&i.Visibility,
		&i.Status,
		&i.MaxConcurrentTasks,
		&i.OwnerID,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Description,
		&i.RuntimeID,
		&i.Instructions,
		&i.ArchivedAt,
		&i.ArchivedBy,
		&i.CustomEnv,
		&i.CustomArgs,
		&i.McpConfig,
		&i.Model,
		&i.ThinkingLevel,
		&i.ComposioToolkitAllowlist,
		&i.PermissionMode,
		&i.Kind,
		&i.SystemKey,
		&i.DisabledRuntimeSkills,
		&i.ServiceTier,
		&i.ConversationStarters,
	)
	return i, err
}
